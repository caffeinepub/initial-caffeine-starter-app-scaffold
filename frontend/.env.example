# AI key is now hardcoded in frontend/src/lib/aiEngine.ts for out-of-the-box functionality.
# No API key configuration is required from the user.
# The app uses Groq's free-tier API (https://console.groq.com) — no credit card needed.
# VITE_GEMINI_API_KEY is no longer used.
